<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Employee</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #6c46a2;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background: #429E9D;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            width: 400px;
            text-align: center;
        }
        h1 {
            color: #333;
        }
        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        label {
            font-weight: bold;
            margin-top: 10px;
        }
        input {
            width: 90%;
            padding: 8px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px 15px;
            cursor: pointer;
            border-radius: 5px;
            margin-top: 15px;
        }
        button:hover {
            background-color: #218838;
        }
        p {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Update Employee</h1>
        <% if(record){ %>
            <form action="/myemp/update" method="post" enctype="multipart/form-data">
                <input type="hidden" name="eno" value="<%= record.eno %>">
            
                <label for="ename">Employee Name:</label>
                <input type="text" id="ename" name="ename" value="<%= record.ename %>" required>
            
                <label for="esal">Employee Salary:</label>
                <input type="number" id="esal" name="esal" value="<%= record.esal %>" required>

                <label for="emp_mail">Mail Id:</label>
                <input type="email" id="emp_mail" name="emp_mail" value="<%= record.emp_mail %>" required>

            
                <label for="egrade">Employee Grade:</label>
                <input type="text" id="egrade" name="egrade" value="<%= record.egrade %>" required>
            
                <label for="emp_photo">Update Employee Photo:</label>
                <input type="file" id="emp_photo" name="emp_photo" accept="image/*">
            
                <% if (record.emp_photo) { %>
                    <img src="/photo/<%= record.eno %>" alt="Employee Photo" width="100">
                <% } %>
            
                <button type="submit"> Update </button>
            </form>
            
        <% } else { %>
        <p>No record found with the given employee number</p>
        <% } %>
    </div>
</body>
</html>
