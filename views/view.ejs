<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Details</title>
    
    <style>
        body { 
        font-family: Arial, sans-serif; 
        text-align: center; 
        }
        .container { 
        width: 50%; 
        margin: auto;
        padding: 20px; 
        border: 1px solid #ccc; 
        border-radius: 10px; 
        }
        img { 
        width: 150px; 
        height: 150px; 
        border-radius: 50%; 
        }
        .btn { 
        padding: 10px 20px; 
        background-color: green; 
        color: white; 
        text-decoration: none; 
        border-radius: 5px; 
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Employee Details</h2>
        <p><strong>Employee No:</strong> <%= employee.eno %></p>
        <p><strong>Name:</strong> <%= employee.ename %></p>
        <p><strong>Salary:</strong> <%= employee.esal %></p>
        <p><strong>Grade:</strong> <%= employee.egrade %></p>
        <p><strong>Email:</strong> <%= employee.emp_mail %></p>
        <% if (employee.emp_photo) { %>
            <img src="<%= employee.emp_photo %>" alt="Employee Photo">
        <% } %>
        <br><br>
        <a href="/myemp/download/<%= employee.eno %>" class="btn">Download PDF</a>
    </div>
</body>
</html>
